import { StaticImageData } from "next/image";


export type SkillIcon = {
    id: number;
    title: string;
    icon: any;
};
export type Project = {
    id: number,
    title: string,
    documenting: string;
    image: any;
    href: string
} 
